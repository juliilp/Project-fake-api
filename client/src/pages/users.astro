---
import Layout from "../layouts/Layout.astro";
import arrayUsers from '../helpers/arrayUsers'
import ComponenteObjeto from "../components/componenteObjeto.astro";
---

<Layout title="Usuarios" >
    <main class="flex flex-col gap-8 justify-center items-center  " >
        <h1 class="font-bold text-4xl sm:text-5xl font-sans" >Users</h1>
        {
            arrayUsers.map((user,key) => {
                return <ComponenteObjeto
                key={key}
                titulo={user.titulo}
                descripcion={user.descripcion}
                request={user.request}
                response={JSON.stringify(user.response)}
                />
            })
        }
    </main>
</Layout>